{
  "name": "portfolio-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix portfolio-backend\" \"npm run dev --prefix portfolio-web\"",
    "install": "npm install --prefix portfolio-web && npm install --prefix portfolio-backend",
    "build": "npm run build --prefix portfolio-web && rm -rf dist && cp -R portfolio-web/dist ./dist"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "fs-extra": "^11.3.3",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.11.0",
    "resend": "^6.9.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
